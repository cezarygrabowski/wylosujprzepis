{% extends '::base1.html.twig' %}


{% block FlashBag %}
    {% for type, flashes in app.session.flashbag.all %}
        {% for flash in flashes %}
            <div class="alert alert-{{ type }} alert-position">
                {{ flash }}
            </div>

        {% endfor %}
    {% endfor %}
{% endblock %}
